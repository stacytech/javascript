const x = 10.6;
const y = 20.4;
const z = 30.9;

const max = Math.max(x,y,z);
const min = Math.max(x,y,z);

const randomNumber = Math.random()
console.log(Math.ceil (randomNumber));
console.log(Math.floor(randomNumber));